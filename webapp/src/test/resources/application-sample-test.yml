app:
  version: @app.version@

spring:
  datasource:
    url: jdbc:mysql://localhost:3307/
    username:
    password:

mybatis:
  mapper-locations: classpath*:/db/mapper/*.xml

document:
  path: documents

openai:
  endpoint: endpoint
  apikey: apikey
  model: model