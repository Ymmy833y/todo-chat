app:
  version: @app.version@

spring:
  datasource:
    url: jdbc:mysql://localhost:3307/
    username:
    password:

mybatis:
  mapper-locations: classpath*:/db/mapper/*.xml

openai:
  endpoint: endpoint
  apikey: apikey
  model: model